import(f, src.main, 1);
import(g, src.main, 1);
import(h, src.main, 1);


fn hanoi_moves(a, b, c) {
  if len(c) == 5 {
    []
  }
  else {
    if first(a) > first(b) {
      foo;
    }
    else {
      move(a, b);
    }
  }
}

